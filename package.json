{
  "name": "uniswap-eth-usdc-analyzer",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ uniswap-eth-usdc-analyzer",
    "create-local": "graph create --node http://localhost:8020/ uniswap-eth-usdc-analyzer",
    "remove-local": "graph remove --node http://localhost:8020/ uniswap-eth-usdc-analyzer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 uniswap-eth-usdc-analyzer",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.58.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
